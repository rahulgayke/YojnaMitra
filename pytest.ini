[pytest]
# When tests run from the repository root, load this checkout rather than an
# older YojanaMitra package installed in the active Python environment.
pythonpath = backend/src
testpaths = backend/tests
addopts = -q
